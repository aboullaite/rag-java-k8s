# GCP Project Configuration
GCP_PROJECT=your-gcp-project-id

# GKE Cluster Configuration
GKE_REGION=europe-west4
CLUSTER_NAME=rag-demo

# Container Registry
# Note: Registry location doesn't need to match GKE region
REGISTRY=europe-north1-docker.pkg.dev/your-gcp-project-id/rag-demo

# GPU Configuration
# Machine types and accelerators available per region:
# - europe-west4: g2-standard-4 with nvidia-l4
# - us-central1: g2-standard-4 with nvidia-l4
# - europe-north1: a3-highgpu-1g with nvidia-h100-80gb
GPU_MACHINE_TYPE=g2-standard-4
GPU_ACCELERATOR=nvidia-l4
GPU_MIN_NODES=0
GPU_MAX_NODES=1

# HuggingFace Token (required for gated models like Gemma)
# Get from: https://huggingface.co/settings/tokens
HF_TOKEN=your-huggingface-token